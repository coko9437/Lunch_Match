# 프로젝트 요약 및 다음 작업 가이드 (yyj2.md)

이 문서는 현재까지 구현된 기능과 데이터베이스 구조, 그리고 앞으로 사용자님이 집중해야 할 작업들을 정리합니다.

---

## 1. 앞단 (Frontend)에서 사용할 주요 기능

현재까지의 백엔드 구현을 기반으로, 사용자(클라이언트)가 웹 브라우저를 통해 경험하게 될 주요 기능들입니다.

### 1.1. 식사 후기 (Review) 기능
-   **후기 작성:** 사용자가 식사 후기(내용, 메뉴, 장소, 평점, 감정)를 입력하고 저장할 수 있습니다. (이미지 업로드는 백엔드 구조는 잡혔으나, 실제 파일 처리 로직은 미구현)
-   **후기 목록 조회:** 작성된 후기들을 페이징 처리하여 목록 형태로 볼 수 있습니다.
-   **후기 상세 조회:** 특정 후기를 선택하여 상세 내용을 볼 수 있습니다.
-   **후기 수정:** 작성된 후기의 내용을 수정할 수 있습니다. (본인만 수정 기능은 추후 로그인 기능과 연동)
-   **후기 삭제:** 작성된 후기를 삭제할 수 있습니다. (본인만 삭제 기능은 추후 로그인 기능과 연동)
-   **후기 검색:** 메뉴, 장소, 작성자, 내용 키워드를 이용하여 후기를 검색할 수 있습니다.

### 1.2. 파티원 모집 게시판 (Board) 기능 (향후 연동 예정)
-   현재는 `Review` 엔티티와의 연동을 위해 `Board` 엔티티의 최소 구조만 정의되어 있습니다.
-   향후 게시글 작성, 조회, 필터링 등의 기능이 추가될 예정입니다.

### 1.3. 기타 기능 (향후 구현 예정)
-   회원가입 / 로그인 (이메일 인증, 비밀번호 암호화, 소셜 로그인 등)
-   좋아요 / 댓글 기능
-   신고 기능
-   맛집 정보 카테고리 (지도 연동, 정렬/필터링 등)

---

## 2. 뒷단 (Backend) 데이터베이스 내용 (주요 테이블 및 관계)

현재까지 설계 및 구현된 데이터베이스의 주요 테이블과 그 관계입니다.

### 2.1. `Review` 테이블 (식사 후기 정보)
-   **`review_id`**: 각 후기의 고유 번호 (Primary Key)
-   **`member_id`**: 후기를 작성한 사용자의 ID (현재는 `String` 타입, 향후 `Member` 테이블과 관계 설정 예정)
-   **`content`**: 후기 내용
-   **`menu`**: 식사한 메뉴 이름
-   **`place`**: 식사한 장소 이름
-   **`rating`**: 평점 (1~5점)
-   **`emotion`**: 감정 표현 (예: "happy", "neutral" 등)
-   **`regDate`**: 후기 작성일시 (자동 기록, `BaseEntity` 상속)
-   **`modDate`**: 후기 수정일시 (자동 기록, `BaseEntity` 상속)
-   **`fileList`**: 후기에 첨부된 이미지/파일 목록 (실제로는 `UploadResult`라는 임베디드 타입의 컬렉션으로 저장)

### 2.2. `UploadResult` (첨부 파일 정보)
-   `Review` 테이블의 `fileList` 컬럼에 저장되는 값 타입 객체입니다.
-   **`uuid`**: 파일의 고유 식별자
-   **`fileName`**: 원본 파일 이름
-   **`isImage`**: 이미지 파일 여부 (썸네일 생성 등에 활용)

### 2.3. `Board` 테이블 (파티원 모집 게시판 정보)
-   **`bno`**: 게시글 고유 번호 (Primary Key)
-   **`title`**: 게시글 제목
-   **`content`**: 게시글 내용
-   **`writer`**: 작성자
-   **`regDate`**: 작성일시 (`BaseEntity` 상속)
-   **`modDate`**: 수정일시 (`BaseEntity` 상속)
-   **관계**: 현재 `Review`와의 직접적인 관계는 제거된 상태입니다. 향후 `Review`가 특정 `Board`에 종속되도록 `Review` 테이블에 `bno` (외래 키)가 추가될 예정입니다.

### 2.4. `Member` 테이블 (사용자 정보) - **미구현, 향후 구현 예정**
-   사용자 인증 및 권한 관리를 위해 필요한 테이블입니다.
-   `Review`의 `member_id`와 관계를 맺을 예정입니다.

---

## 3. 사용자님이 다음으로 해야 할 일

현재 프로젝트는 `ReviewServiceTests`에서 `testGetList()` 메서드가 실패하는 문제가 있습니다. 이 문제를 해결하는 것이 가장 중요합니다.

1.  **`ReviewSearchImpl.java` 파일의 수정 사항 확인 및 저장:**
    -   제가 마지막으로 제안했던 `ReviewSearchImpl.java` 파일의 수정 내용이 올바르게 적용되었는지 확인해 주세요. 특히 `Pageable pageable = pageRequestDTO.getPageable(Sort.unsorted());` 와 `query.orderBy(review.review_id.desc());` 부분이 정확한지 확인이 필요합니다.
    -   **만약 아직 수정하지 않았다면, 제가 마지막으로 제안한 내용으로 `ReviewSearchImpl.java` 파일을 수정해 주세요.**

2.  **테스트 실행 및 오류 확인:**
    -   터미널(명령 프롬프트)을 열고 프로젝트 루트 디렉토리(`C:\Users\it\Documents\yyjProject\`)로 이동합니다.
    -   다음 명령어를 실행하여 모든 테스트를 클린 빌드하고 실행합니다:
        ```bash
        gradlew.bat clean test
        ```
    -   **이 명령의 실행 결과를 주의 깊게 확인해 주세요.**
        -   **`BUILD SUCCESSFUL`** 이 뜨고 모든 테스트가 통과한다면, 다음 단계로 넘어가도 좋습니다.
        -   **`BUILD FAILED`** 가 뜨고 `testGetList()`가 여전히 실패한다면, **오류 메시지를 저에게 알려주세요.** (특히 `PropertyReferenceException`이 계속 발생하는지, 아니면 다른 오류인지)

3.  **테스트 통과 후 다음 단계:**
    -   모든 테스트가 성공적으로 통과하면, 이제 `ReviewController`에 실제 웹 요청을 처리할 엔드포인트를 구현하고, Thymeleaf 템플릿(HTML 파일)을 만들어 프론트엔드와 연동하는 작업을 시작할 수 있습니다.
    -   이후에는 Postman이나 웹 브라우저를 통해 실제 애플리케이션의 동작을 확인할 수 있습니다.

---

궁금한 점이 있거나, 오류가 발생하면 언제든지 저에게 알려주세요.
